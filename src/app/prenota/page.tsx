/**
 * Pagina Prenota: form multi-step con validazioni e riepilogo.
 * TODO: implementare BookingWizard con react-hook-form + zod.
 */
export default function PrenotaPage() {
  return (
    <main id="main" className="container" aria-labelledby="booking-title">
      <h1 id="booking-title">Prenota</h1>
      <p>Completa i passaggi per richiedere la prenotazione.</p>
      {/* <BookingWizard /> */}
      <p>TODO: implementare step (Data/Orario → Persone/Tipologia → Dettagli → Riepilogo & Consensi)</p>
    </main>
  );
}
