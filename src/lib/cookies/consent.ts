/**
 * TODO: serializzare consenso in cookie con versioning (es. consent_v1.0.0).
 * Integrare con middleware o route handlers per blocco server-side se necessario.
 */
export function parseConsentCookie(_cookieHeader?: string) {
  // TODO: parsing reale del cookie di consenso (v1)
  return null;
}
